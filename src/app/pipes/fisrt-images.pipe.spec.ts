import { FisrtImagesPipe } from './fisrt-images.pipe';

describe('FisrtImagesPipe', () => {
  it('create an instance', () => {
    const pipe = new FisrtImagesPipe();
    expect(pipe).toBeTruthy();
  });
});
