import { NamesfilterPipe } from './namesfilter.pipe';

describe('NamesfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NamesfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
